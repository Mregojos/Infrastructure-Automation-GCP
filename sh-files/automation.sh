# Create a compute engine instance
    # No Service Account
# Download container
# Create a database by running run database image
# Create a static external ip address
# Use the static address to the compute engine
# Use these credentials to connect to the database

# App in a repository
# Build an image
# Submit it to the  the Artifact Registry
# Create a service account to be used
# Deploy using Cloud Run